[
  {
    "name": "delegate_to_agent",
    "description": "Hand off a self-contained sub-task to a separate local AI agent that runs in its own session. ASYNCHRONOUS: returns a task_id right away and the sub-agent keeps working in the background — this call never blocks. So you can fan out several delegations at once and keep working, then collect the results with get_delegation_status by passing the task_ids array (one id to poll a single task, or many at once to poll the whole fan-out in one call) — or stop one early with cancel_delegation(task_id). The sub-agent CANNOT see this conversation, your open files, or earlier turns — it starts cold, so `task` must carry everything it needs. Best for independent, parallelizable work you can describe up front; not for steps that need your ongoing back-and-forth.",
    "inputSchema": {
      "type": "object",
      "required": [
        "agent_type",
        "task"
      ],
      "properties": {
        "agent_type": {
          "type": "string",
          "enum": [
            "claude_code",
            "codex",
            "open_code",
            "gemini",
            "cline",
            "open_claw",
            "hermes",
            "code_buddy",
            "kimi_code",
            "pi"
          ],
          "description": "Which local agent runs the sub-task. Pick the one best suited to the work."
        },
        "task": {
          "type": "string",
          "description": "The complete, self-contained prompt for the sub-agent. It has no access to this conversation or your context, so spell out everything: the goal, the relevant background and absolute file paths, any constraints, and exactly what to return."
        },
        "working_dir": {
          "type": "string",
          "description": "Absolute path the sub-agent runs in. Defaults to this session's working directory."
        }
      }
    }
  },
  {
    "name": "get_delegation_status",
    "description": "Collect delegated task results — or check on them — by the task_ids from delegate_to_agent. Pass a single id in the array to poll one task, or many at once to poll a whole fan-out in a single call. The response is ALWAYS a JSON object {\"tasks\":[...]} with one entry per id, in the order you asked — one entry for a single id, several for a fan-out; every entry carries that task's task_id and status, so you can always tell which task it is and where it stands. The wait_ms argument picks the mode: omit it for a non-blocking snapshot (poll), or pass wait_ms to wait — wait_ms>0 blocks up to that many ms (capped at 60000; re-call to keep waiting), wait_ms=0 blocks with no timeout for as long as the sub-agent runs. When polling several, the wait returns as soon as ANY requested task reaches a terminal state, so re-call to keep collecting the rest. When you just need the results, prefer blocking over repeated polling. Each task reports its current state — running, or a terminal completed / failed / canceled (or unknown if the id isn't recognized); a finished task includes its full result text. While tasks are still running and you are simply waiting, do NOT emit any user-facing message between calls — just call again silently. Skip status narration like \"still running\", \"not returned yet\", or \"continuing to wait\"; it fragments the conversation. Speak to the user only when there is a terminal result to report or something genuinely needs their input.",
    "inputSchema": {
      "type": "object",
      "required": [
        "task_ids"
      ],
      "properties": {
        "task_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "One or more task_ids returned by delegate_to_agent — pass a single id to poll one task, or several to poll them together in one call. The response is always a JSON object {\"tasks\":[...]} with one entry per id (in this order) — one entry for a single id — and each entry carries that task's task_id and status. With wait_ms set, the call returns as soon as ANY of these tasks reaches a terminal state."
        },
        "wait_ms": {
          "type": "integer",
          "minimum": 0,
          "description": "How long to wait. Omit for an immediate snapshot (poll). A positive value blocks up to that many milliseconds (capped at 60000). 0 means no timeout — block until a task reaches a terminal state, however long it takes. When several ids are given, the wait resolves as soon as ANY requested task becomes terminal."
        }
      }
    }
  },
  {
    "name": "cancel_delegation",
    "description": "Stop a running delegated task by its task_id and tear down its sub-agent — use this only when you no longer want the result. Don't cancel just because a task is taking a while: substantial work can legitimately run for a long time, so prefer waiting on it (get_delegation_status with wait_ms) over canceling. If the task has already finished, nothing is canceled and its final result is returned instead.",
    "inputSchema": {
      "type": "object",
      "required": [
        "task_id"
      ],
      "properties": {
        "task_id": {
          "type": "string",
          "description": "The task_id returned by delegate_to_agent."
        }
      }
    }
  },
  {
    "name": "check_user_feedback",
    "description": "IMPORTANT: This tool gives you the ability to receive the user's real-time guidance while you work, preventing you from going down the wrong path and wasting effort. The user can type corrections, new requirements, or hints at any time during your turn — but you will NEVER see them unless you call this tool. Without it, you could spend minutes on work the user has already tried to redirect.\n\nWhen to call:\n- After thinking through your approach but BEFORE starting implementation — the user may have clarified requirements.\n- Before making a significant decision (choosing an architecture, picking which files to modify, deciding on an approach) — the user may prefer a different direction.\n- After completing a sub-task and before moving to the next step — the user may want to adjust priorities.\n- When you pause to plan or organize your next steps — a natural checkpoint to read any pending guidance.\n\nThis is a quick, non-blocking snapshot that returns instantly. It either returns the user's messages (treat as HIGH-PRIORITY steering and adjust immediately) or an empty result (continue your current plan). Call it proactively — do not wait to be asked.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "ask_user_question",
    "description": "Ask the user one or more multiple-choice questions and BLOCK until they answer. The questions render as interactive cards above the user's input box; this call returns only once the user submits their choices (or dismisses the card). Use ONLY when you are blocked on a decision that is genuinely the user's to make — one you cannot resolve from the request, the code, or sensible defaults (e.g. which approach, which format, which library, which of several files). Do NOT use it to ask 'should I proceed?' or to confirm an obvious default, and do NOT use it for open-ended questions that aren't a small set of discrete choices. The user can always pick 'Other' and type a custom answer, so don't add your own 'Other' option. If you want to recommend a choice, put it first and append ' (Recommended)' to its label. Prefer ONE call with a few related questions over many sequential calls.",
    "inputSchema": {
      "type": "object",
      "required": [
        "questions"
      ],
      "properties": {
        "questions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 4,
          "description": "1-4 questions to ask in this single call.",
          "items": {
            "type": "object",
            "required": [
              "question",
              "header",
              "multiSelect",
              "options"
            ],
            "properties": {
              "question": {
                "type": "string",
                "description": "The full question shown to the user."
              },
              "header": {
                "type": "string",
                "maxLength": 12,
                "description": "Short category label (≤ 12 chars) shown as a chip, e.g. 'Approach', 'Format'."
              },
              "multiSelect": {
                "type": "boolean",
                "description": "If true, the user may select multiple options; otherwise it's a single choice."
              },
              "options": {
                "type": "array",
                "minItems": 2,
                "maxItems": 4,
                "description": "2-4 selectable choices.",
                "items": {
                  "type": "object",
                  "required": [
                    "label",
                    "description"
                  ],
                  "properties": {
                    "label": {
                      "type": "string",
                      "description": "Concise choice text. Append ' (Recommended)' to the first option to recommend it."
                    },
                    "description": {
                      "type": "string",
                      "description": "What this choice means or its trade-off."
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  {
    "name": "get_session_info",
    "description": "Look up a session/conversation the user referenced in their message and return what it is: its title, which agent ran it, its status, workspace folder, git branch, model, message count, and token-usage stats — and, on request, a compact view of its most recent messages. Call this whenever the user's message contains a `veryagent://session/<number>` reference (a mentioned session) and you need to know what that session is about or what happened in it (e.g. to continue its work or summarize it). Pass the NUMBER from the reference as `session_id` (it is veryagent's internal conversation id, NOT the agent's own session id). The result is read-only and never modifies the session. A session that no longer exists returns a `found: false` result you can act on — it is not an error.",
    "inputSchema": {
      "type": "object",
      "required": [
        "session_id"
      ],
      "properties": {
        "session_id": {
          "type": "integer",
          "description": "The number in the `veryagent://session/<number>` reference from the user's message (veryagent's internal conversation id). For example, given `[Fix auth](veryagent://session/214)`, pass 214."
        },
        "max_messages": {
          "type": "integer",
          "minimum": 0,
          "description": "How many of the session's most recent turns to include as compact text. Defaults to 20; pass 0 for metadata only (faster, no transcript read). Capped at 200."
        }
      }
    }
  },
  {
    "name": "vision_analyze",
    "description": "Analyze an image using a vision-capable AI model. Send an image (as base64 data or a local file path) and a text prompt describing what you want to know about the image. The vision model returns a detailed text description you can use in your work.\n\nWhen to use:\n- Understand a screenshot or image the user sent\n- Read UI elements, error messages, or dialog boxes from a screenshot\n- Analyze charts, diagrams, or visual data\n- Identify what is shown on the desktop, a browser page, or application window\n- Interpret any visual content your text-only model cannot directly process\n\nThe tool handles image loading for you — pass either base64-encoded bytes (image_data) or a local file path (image_path). If you can capture a screenshot via shell commands (e.g. `screencapture` on macOS, `cmd /c ...` on Windows), pass the resulting file path.",
    "inputSchema": {
      "type": "object",
      "required": [
        "prompt"
      ],
      "properties": {
        "image_data": {
          "type": "string",
          "description": "Base64-encoded image data. Use this when you already have the image bytes (e.g. from another tool's output). Omit if using image_path instead."
        },
        "image_path": {
          "type": "string",
          "description": "Absolute file path to a local image file. Use this when the image is on disk (e.g. a screenshot you just captured). Omit if using image_data instead."
        },
        "mime_type": {
          "type": "string",
          "description": "MIME type of the image (e.g. image/png, image/jpeg). Required when using image_data; inferred from the file extension when using image_path.",
          "default": "image/png"
        },
        "prompt": {
          "type": "string",
          "description": "What you want to know about the image. Be specific: 'Describe the UI elements and their layout', 'What error message is shown in this dialog?', 'Identify the data in this chart', 'What does this screenshot show?'."
        }
      }
    }
  },
  {
    "name": "generate_image",
    "description": "Platform image generation (VeryAgent built-in). Call this tool with ONLY the user's prompt text. Do NOT pass chat models (deepseek/gpt-4o/step-*) as `model` — omit `model` entirely so settings supply the image model. Do NOT write Python/curl. Do NOT decode base64. Do NOT save files yourself. The VeryAgent chat UI renders the image automatically when the tool succeeds. On success, reply with a short natural-language confirmation only. On error, report the error briefly; do not claim the UI cannot render images.",
    "inputSchema": {
      "type": "object",
      "required": [
        "prompt"
      ],
      "properties": {
        "prompt": {
          "type": "string",
          "description": "The user's original request text. MUST be passed verbatim — never translate, rephrase, or optimize."
        },
        "model": {
          "type": "string",
          "description": "LEAVE OMITTED. The platform uses Image Generation settings. Never pass a chat/completion model name here."
        },
        "image_size": {
          "type": "string",
          "enum": [
            "1K",
            "2K",
            "4K"
          ],
          "description": "Output image resolution. Default is 2K.",
          "default": "2K"
        },
        "aspect_ratio": {
          "type": "string",
          "enum": [
            "1:1",
            "4:3",
            "16:9",
            "9:16",
            "3:4"
          ],
          "description": "Output image aspect ratio. Default is 1:1.",
          "default": "1:1"
        },
        "ref_urls": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Reference image URLs or local paths for image-to-image generation."
        }
      }
    }
  },
  {
    "name": "modify_image",
    "description": "Platform image modification (VeryAgent built-in). Pass the user's edit request and previous image path/URL in ref_urls. Omit `model`. The chat UI displays the result automatically — do NOT paste base64/JSON/paths into your reply.",
    "inputSchema": {
      "type": "object",
      "required": [
        "prompt"
      ],
      "properties": {
        "prompt": {
          "type": "string",
          "description": "The user's modification request text. MUST be passed verbatim — never translate, rephrase, or optimize."
        },
        "model": {
          "type": "string",
          "description": "LEAVE OMITTED. The platform uses Image Generation settings. Never pass a chat/completion model name here."
        },
        "ref_urls": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The previous image URL(s) or path(s) to use as reference for modification."
        }
      }
    }
  },
  {
    "name": "web_search",
    "description": "Search the web for up-to-date information. Returns Markdown-formatted results with titles, sources, dates, and snippets. Best for Chinese content (CSDN, Juejin, Zhihu, etc.) — better coverage than general-purpose search engines. Use this when you need current information beyond your knowledge cutoff. The user's search query is passed verbatim — never translate, rephrase, or optimize it.",
    "inputSchema": {
      "type": "object",
      "required": [
        "query"
      ],
      "properties": {
        "query": {
          "type": "string",
          "description": "The user's search query. MUST be passed verbatim — never translate, rephrase, or optimize."
        },
        "freshness": {
          "type": "string",
          "enum": [
            "day",
            "week",
            "month"
          ],
          "description": "Time filter. Use 'day' when the user asks about today, 'week' for recent/this week, 'month' for this month. Omit for no time restriction."
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20,
          "description": "Number of results to return. Default is 10.",
          "default": 10
        }
      }
    }
  },
  {
    "name": "image_search",
    "description": "Search for images by keyword. Returns JSON with image URLs, dimensions, and match scores. Use this when the user asks to find pictures ('找一张...的图片', '有没有...的图'). The user's search query is passed verbatim — never translate or rephrase.",
    "inputSchema": {
      "type": "object",
      "required": [
        "query"
      ],
      "properties": {
        "query": {
          "type": "string",
          "description": "The user's search query. MUST be passed verbatim — never translate, rephrase, or optimize."
        }
      }
    }
  },
  {
    "name": "team_get_members",
    "description": "List the members of your team (the team you lead as project manager). Returns each member's display name, agent type, slot id, roles, and status. Use this to see who is available before assigning tasks. Only works inside a team leader conversation.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "team_assign_task",
    "description": "Assign a task to a member of your team and make them start working immediately. The member agent is spawned in the team's workspace and receives a role + task briefing as its first message. Returns the created task id and the member's conversation id. After assigning, track progress with team_get_tasks. You are the project manager — do NOT do the members' work yourself; break the boss's request into subtasks and assign them out. Ask the boss for confirmation via ask_user_question before assigning.",
    "inputSchema": {
      "type": "object",
      "required": [
        "slot_id",
        "subject"
      ],
      "properties": {
        "slot_id": {
          "type": "string",
          "description": "The slot id of the member to assign to — from team_get_members."
        },
        "subject": {
          "type": "string",
          "description": "The task subject/title. Keep it short and actionable."
        },
        "description": {
          "type": "string",
          "description": "Optional detailed task description / requirements for the member."
        }
      }
    }
  },
  {
    "name": "team_get_tasks",
    "description": "Show your team's task board: every task with its subject, owner slot, status (pending / in_progress / completed / failed), and the member's final result text when finished. Use this to track progress after assigning tasks and to report results to the boss.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  }
]
